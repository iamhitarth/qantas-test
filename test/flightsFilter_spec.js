let assert = require('assert');

describe('Flight filter will return', () => {

  it('JSON object with flights key with array of flights.', () => {});

  it('a filtered list of flights.', () => {});

  it('just the expected fields.', () => {});
});
